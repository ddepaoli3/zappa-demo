# Starting
```sh
sudo apt-get update
sudo apt-get install --yes python-pip
sudo pip install virtualenvwrapper
export WORKON_HOME=~/Envs
mkdir -p $WORKON_HOME
source /usr/local/bin/virtualenvwrapper.sh
mkvirtualenv demozappa
```